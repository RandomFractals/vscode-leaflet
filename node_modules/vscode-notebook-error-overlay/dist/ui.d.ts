import { FunctionComponent } from 'preact';
export declare const enum ErrorSource {
    Runtime = 0,
    Compilation = 1
}
export declare const Overlay: FunctionComponent<{
    close(): void;
    source: ErrorSource;
    errors: ReadonlyArray<string>;
}>;
